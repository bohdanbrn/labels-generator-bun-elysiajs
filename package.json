{
    "name": "generate-pdf",
    "module": "index.ts",
    "type": "module",
    "scripts": {
        "start": "bun run index.ts",
        "start:dev": "bun run --watch index.ts",
        "start:dev:hot": "bun run --hot index.ts"
    },
    "devDependencies": {
        "bun-types": "^1.0.7"
    },
    "peerDependencies": {
        "typescript": "^5.0.0"
    }
}
